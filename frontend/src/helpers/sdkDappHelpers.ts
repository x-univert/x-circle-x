// Export des helpers SDK MultiversX v5
// Note: logout et refreshAccount ne sont plus disponibles directement dans sdk-dapp v5
// Utilisez provider.logout() via getAccountProvider() pour la deconnexion
export { getAccountProvider } from 'lib';
